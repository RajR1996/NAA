@model NAA.Data.University

@{
    ViewBag.Title = "DeleteUniversity";
}

<h2>DeleteUniversity</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>@*University*@</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.UniversityName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.UniversityName)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

    <div class="form-actions no-color">
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        @Html.ActionLink("Back to University List", "GetUniversityList", new { Controller = "University" }, new { @class = "btn btn-default" })
    </div>
    }
</div>
